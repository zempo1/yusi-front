worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    # 开启 Gzip 压缩，减少传输体积
    gzip on;
    gzip_min_length 1k;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/javascript application/json application/javascript application/x-javascript application/xml;

    server {
        listen       8080;
        server_name  localhost;

        # 前端静态文件目录
        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
            # 核心配置：解决 Vue/React 路由 History 模式刷新 404 的问题
            try_files $uri $uri/ /index.html;
        }

        # 后端 API 代理
        location /api/ {
            proxy_pass http://yusi:611/api/;
            proxy_buffering off;
            proxy_set_header Host $host;
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Origin,Authorization,Accept,X-Requested-With,Content-Type' always;
        }

        # ========== 高德地图安全代理 (推荐方式) ==========
        # 自定义地图样式代理
        location /_AMapService/v4/map/styles {
            set $args "$args&jscode=$AMAP_SECURITY_CODE";
            proxy_pass https://webapi.amap.com/v4/map/styles;
        }

        # Web 服务 API 代理 (POI搜索、地理编码等)
        location /_AMapService/ {
            set $args "$args&jscode=$AMAP_SECURITY_CODE";
            proxy_pass https://restapi.amap.com/;
        }

        # 错误页面处理
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
}